{% extends 'QandaHomeBundle::layout.html.twig' %}

{% block body %}
<h3>Асуулт оруулах</h3>

{{ form_start(question_form, {'attr': {'novalidate': 'novalidate'}}) }}
{{ form_errors(question_form) }}

    {{ form_row(question_form.title) }}
    {{ form_row(question_form.question) }}

    <input type="submit" value="Оруулах" class="btn btn-primary">
{{ form_end(question_form) }}
{% endblock %}
